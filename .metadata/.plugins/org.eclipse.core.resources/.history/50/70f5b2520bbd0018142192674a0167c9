package tv.business;

import java.util.List;

public interface ShowDAO<T> {
    T get(String genre);
    T get(int length);
    List<T> getAll();
    boolean add(T t);
    boolean update(T t);
    boolean delete(T t);
}